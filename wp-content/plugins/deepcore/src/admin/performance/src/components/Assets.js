const Assets = () => {
    return (
        <>
            <h3>(Pro)</h3>
            <p>In this section you can prevent the CSS and JS files from loading in the frontend.</p>
            <small>By selecting each item the script or style will be removed from rendering on the website.</small>
        </>
    )
}

export default Assets
